这个交互很合理，而且更“桌宠”：默认左键就是“拖动整只角色(=拖窗口)”，Alt 才进入“精调模型”。

当前落地逻辑：

- **Wheel**：以窗口中心为锚点缩放 AvatarWindow（模型显示随之同步变大/变小）
- **Alt+Wheel**：只缩放模型，不缩放窗口（沿用 VRM scale）
- **Left Drag**：拖动窗口（角色一起走）
- **Alt+Left Drag**：只拖动模型（窗口不动）

对应实现：

- 前端交互分流：`src/components/vrm/useVrmRenderer.ts`
- 像素级命中/穿透：Windows 采用 cursor gate（`set_ignore_cursor_events` 动态切换），已移除 `WM_NCHITTEST` 路径：`src-tauri/src/windows/avatar_window.rs`
