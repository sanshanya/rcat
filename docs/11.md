这个交互很合理，而且更“桌宠”：默认左键就是“拖动整只角色(=拖窗口)”，Alt 才进入“精调模型”。

我已经按你的逻辑落地了：

* **Wheel**：以**窗口中心为锚点**缩放 AvatarWindow（模型显示随之同步变大/变小）；**Alt+Wheel**：只缩放模型不缩放窗口（沿用现有 VRM scale）。见 **AvatarRoot.tsx (line 99)**（阈值/上下限在 **AvatarRoot.tsx (line 28)**）。
* **Left Drag**：拖动窗口（角色一起走）；**Alt+Left Drag**：只拖动模型（窗口不动）。见 **AvatarRoot.tsx (line 82)**。
* 为了让 **Alt+…** 真能到前端，我把 **WM_NCHITTEST** 里原来的 **Alt -> HTCAPTION** 去掉了：模型像素命中只返回 **HTCLIENT**。见 **avatar_window.rs (line 173)**。
