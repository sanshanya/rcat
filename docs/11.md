这个交互很合理，而且更“桌宠”：默认左键就是“拖动整只角色(=拖窗口)”，需要精调时切到 Model 工具即可。

当前落地逻辑：

- **Tool=Pet**：Wheel 缩放窗口（以窗口中心为锚点）、Left Drag 拖动窗口（角色一起走）
- **Tool=Model**：Wheel 缩放模型、Left Drag 移动模型（窗口不动）
- **Tool=Camera**：OrbitControls（滚轮缩放视角、拖拽旋转/平移）

对应实现：

- 前端交互分流：`src/components/vrm/vrmCanvasInputController.ts`
- 像素级命中/穿透：Windows 采用 cursor gate（`set_ignore_cursor_events` 动态切换），已移除 `WM_NCHITTEST` 路径：`src-tauri/src/windows/avatar_window/service.rs`
