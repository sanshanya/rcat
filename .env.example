# Buncat AI Configuration
# Copy this file to .env and fill in your values

# OpenAI-compatible API base URL
AI_BASE_URL=https://api.deepseek.com

# DeepSeek strict Tool Calls (Beta) use:
# AI_BASE_URL=https://api.deepseek.com/beta

# Optional: force strict tool schemas (1/true/yes/on)
# AI_TOOL_STRICT=1

# Retry settings for network requests (chat streaming / VLM)
# AI_MAX_ATTEMPTS=5
# AI_RETRY_BASE_DELAY_MS=250
# AI_RETRY_MAX_DELAY_MS=4000

# Tool-calling: max tool rounds per request (default 5)
# AI_MAX_TOOL_ROUNDS=15

# Provider hint (optional): openai | deepseek | compatible
AI_PROVIDER=deepseek

# Your API key
AI_API_KEY=sk-your-api-key-here

# Model name
AI_MODEL=deepseek-chat

# ---------------------------------------------------------------------------
# Conversation history storage (Turso / libSQL)
# ---------------------------------------------------------------------------
# When configured, history is persisted to your Turso database.
#
# Create a database with `turso db create ...` and then:
# - Get the URL:    `turso db show <db-name> --url`
# - Get the token:  `turso db tokens create <db-name>`
#
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=your-token
